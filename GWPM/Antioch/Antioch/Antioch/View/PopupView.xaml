<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Antioch.View.PopupView">
  <ContentView.Content>
                <Grid>
                    <Grid.RowDefinitions BackgroundColor="White">
                        <RowDefinition Height="9*" />
                        <RowDefinition Height="1*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10*" />
                    </Grid.ColumnDefinitions>

                    <Frame BorderColor="Gray" Grid.Row="0" Grid.Column="0" BackgroundColor="Beige">
                        <Label x:Name="TextEditor" Text="" TextColor="Black" LineBreakMode="WordWrap"/>
                    </Frame>

                    <Frame BorderColor="Gray" Grid.Row="1" Grid.Column="0">
                             <Button  x:Name="closebutton" Text="닫기" Clicked="OnCloseButtonClicked" Style="{StaticResource buttonStyle2}" />
                    </Frame>

                </Grid>

    </ContentView.Content>
</ContentView>